apply from: "../../module.gradle"

android {

    defaultConfig {
        if (isModule.toBoolean()) {
            applicationId "com.sundy.discover"
        }
    }


}

dependencies {

    implementation 'androidx.legacy:legacy-support-v4:1.0.0'
}
